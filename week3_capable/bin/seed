#!/usr/bin/env bash
# Hand a character prior notes it did not earn itself (week3). Offline, free.
set -euo pipefail
root="$(cd "$(dirname "$0")/../.." && pwd)"
[ -f "$root/.env" ] && { set -a; . "$root/.env"; set +a; }
cd "$root/week3_capable/python"
exec uv run python examples/seed_memory.py "$@"
