#!/usr/bin/env bash
# Turn the agent loose (week3). --dry-run is offline and free.
set -euo pipefail
root="$(cd "$(dirname "$0")/../.." && pwd)"
[ -f "$root/.env" ] && { set -a; . "$root/.env"; set +a; }
cd "$root/week3_capable/python"
exec uv run python examples/grind.py "$@"
