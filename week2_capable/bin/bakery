#!/usr/bin/env bash
# The bakery proof run (week2 memory M4). --fresh wipes memory first (run 1).
set -euo pipefail
root="$(cd "$(dirname "$0")/../.." && pwd)"
[ -f "$root/.env" ] && { set -a; . "$root/.env"; set +a; }
cd "$root/week2_capable/python"
exec uv run python examples/bakery.py "$@"
