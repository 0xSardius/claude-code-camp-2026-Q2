source "https://rubygems.org"

gem "dotenv"

# mud_manager is this project's own reference telnet-session gem
# (week0_explore/mud_manager) -- it has never been published to rubygems.org,
# so the gemspec's plain `add_dependency "mud_manager"` can't resolve on its
# own. Path override for local development.
gem "mud_manager", path: "../../../week0_explore/mud_manager"

gemspec
